@using ToolsApp.Core.Interfaces.Models

<ul>
  @foreach(var color in Colors) {
    <li>@color.Name @color.Hexcode</li>
  }
</ul>

@code {

  [Parameter]
  public IEnumerable<IColor> Colors { get; set; } = new List<Color>();

}